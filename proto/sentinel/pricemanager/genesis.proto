syntax = "proto3";
package sentinel.pricemanager;

import "gogoproto/gogo.proto";
import "sentinel/pricemanager/params.proto";

option go_package = "github.com/sentinel-official/hub/v12/x/pricemanager/types";

// GenesisState defines the interquery module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
  string port_id = 2;
}
